import React from "react";

interface RouteMapProps {
	coordinates: Array<Array<number>>;
}

const RouteMap: React.FC<RouteMapProps> = ({ coordinates }) => {
	return <></>;
};

export default RouteMap;
